# Peable Gateway base URL — REST + socket API this app talks to.
VITE_GATEWAY_URL=https://api.peable.to

# Custom URL scheme the wallet app registers for the "open in wallet" deep link.
VITE_WALLET_DEEPLINK_SCHEME=peable
